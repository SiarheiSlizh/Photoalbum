@using PLMvc.Models;
@model IEnumerable<PhotoViewModel>

<div>
    <h4>PhotoViewModel</h4>
    <hr />
    
    @if (Model.Count() != 0)
    {
        <div id="update" class="container">
            @foreach (var photo in Model)
            {

            }
        </div>

        using (Ajax.BeginForm(new AjaxOptions { InsertionMode = InsertionMode.Replace, UpdateTargetId = "update" }))
        {

        }
    }
    else
    {
        <p>While you have no photos.</p>
    }
</div>
